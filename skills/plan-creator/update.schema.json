{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "PlanUpdate",
  "type": "object",
  "required": [
    "plan_version",
    "plan_markdown",
    "issues",
    "applied",
    "systemic_dispositions",
    "rejected_append"
  ],
  "additionalProperties": false,
  "properties": {
    "plan_version": {
      "type": "integer",
      "minimum": 1,
      "description": "Version after revision (input.plan_version + 1)."
    },
    "plan_markdown": {
      "type": "string",
      "minLength": 1,
      "description": "Full text of the revised plan as markdown."
    },
    "issues": {
      "type": "array",
      "description": "Each original critique issue receives a verdict. Preserve order and ids.",
      "items": {
        "type": "object",
        "required": ["id", "verdict", "verdict_reason", "final_severity", "duplicate_of"],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^C[0-9]+$"
          },
          "verdict": {
            "enum": [
              "accept",
              "reject_hallucinated",
              "reject_out_of_scope",
              "reject_taste",
              "downgrade"
            ],
            "description": "accept — apply it; reject_hallucinated — evidence is false or missing; reject_out_of_scope — outside the plan's scope; reject_taste — taste-only with no justification; downgrade — keep it but lower the severity."
          },
          "verdict_reason": {
            "type": "string",
            "minLength": 1,
            "description": "One sentence. Required for any reject_* and for downgrade."
          },
          "final_severity": {
            "enum": ["blocker", "major", "minor", "nit"],
            "description": "Final severity after judgment. For reject_* it equals the original but does not count toward the stop tally."
          },
          "duplicate_of": {
            "type": "null",
            "description": "Reserved lineage field. It is null for every current creator verdict."
          }
        }
      }
    },
    "applied": {
      "type": "array",
      "description": "Ids of the issues that were applied to the plan.",
      "items": {
        "type": "string",
        "pattern": "^C[0-9]+$"
      }
    },
    "systemic_dispositions": {
      "description": "Exactly one grounded disposition for every accepted or downgraded blocker/major issue. Use an empty array when no such material issue was accepted or downgraded.",
      "$ref": "#/$defs/systemicDispositions"
    },
    "rejected_append": {
      "type": "array",
      "maxItems": 0,
      "description": "Must be empty. Current critic output contains only blocker/major issues; optional improvements are separate opportunities."
    }
  },
  "$defs": {
    "systemicDispositions": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["issue_id", "scope", "rationale", "evidence_refs", "invariant"],
        "properties": {
          "issue_id": { "type": "string", "pattern": "^C[0-9]+$" },
          "scope": { "enum": ["local", "cross-cutting"] },
          "rationale": { "type": "string", "minLength": 1 },
          "evidence_refs": { "$ref": "#/$defs/evidenceRefs" },
          "superseded_by": { "type": ["string", "null"] },
          "invariant": {
            "type": ["object", "null"],
            "additionalProperties": false,
            "required": ["statement", "occurrences"],
            "properties": {
              "statement": { "type": "string", "minLength": 1 },
              "occurrences": {
                "type": "array",
                "minItems": 1,
                "items": {
                  "type": "object",
                  "additionalProperties": false,
                  "required": ["dimension", "subject"],
                  "properties": {
                    "dimension": { "type": "string", "minLength": 1 },
                    "subject": { "type": "string", "minLength": 1 }
                  }
                }
              }
            }
          }
        }
      }
    },
    "evidenceRefs": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["kind"],
        "properties": {
          "kind": { "enum": ["file-line", "plan-section", "phase-gate", "command", "repository", "topology"] },
          "value": { "type": "string" },
          "path": { "type": "string" },
          "line": { "type": "integer", "minimum": 1 },
          "section": { "type": "string" },
          "phase": { "type": "string" },
          "gate": { "type": "string" },
          "command": { "type": "string" },
          "repository": { "type": "string" },
          "topology_id": { "type": "string" }
        }
      }
    }
  }
}
